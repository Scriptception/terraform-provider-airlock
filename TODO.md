# TODO

- Replace JSON bridge attributes for group settings and metarules with typed nested schemas after mutation behavior is validated against an isolated Airlock tenant.
- Add safe mutation acceptance tests for each durable resource in an isolated Airlock tenant.
- Consider Terraform write-only arguments for proxy passwords once the provider depends on a Framework version with write-only schema support.
