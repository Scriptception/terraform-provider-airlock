# TODO

- Verify the remaining granular group-settings write contracts and make each newly
  proven setting writable without reintroducing whole-object updates.
- Add safe mutation acceptance tests for each durable resource in an isolated Airlock
  tenant.
- Replace raw JSON inventory data-source attributes with typed schemas where the public
  response contract is stable.
